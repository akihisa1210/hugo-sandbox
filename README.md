# hugo-sandbox

Sandbox repository for Hugo

## Start the Hugo Server

```
cd quickstart
hugo server -D
```

Access `localhost:1313`.

## Build static Pages

```
cd quickstart
hugo -D
```

Check the `./public` directory.
